<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> | AIWarung</title>
  
  <!-- SEO Meta Tags -->
  <meta name="description" content="AIWarung - Sistem Aplikasi Pengelolaan UMKM Terintegrasi">
  <meta name="author" content="AIWarung Team">

  <!-- Bootstrap 5 CSS -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
  
  <!-- Bootstrap Icons -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
  
  <!-- Custom Styles -->
  <link rel="stylesheet" href="/css/style.css">
  
  <!-- Chart.js for beautiful analytics -->
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
</head>
<body>
  <% if (locals.user) { %>
    <!-- Mobile top navbar -->
    <div class="mobile-top-nav no-print">
      <div class="d-flex align-items-center gap-2">
        <button class="hamburger-btn" id="sidebarToggleMobile" aria-label="Toggle Navigation">
          <i class="bi bi-list"></i>
        </button>
        <span class="fw-bold fs-5">AIWarung</span>
      </div>
      <span class="badge rounded-pill bg-light text-dark px-3 py-2" style="font-size: 11px;"><%= locals.user.roleName %></span>
    </div>
  <% } %>
  <div>
