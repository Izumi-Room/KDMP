<%- include('../../partials/header') %>
<%- include('../../partials/sidebar') %>

<div class="main-wrapper">
  <div class="page-header-nav">
    <div>
      <h1 class="h3 fw-bold mb-0">Tambah UMKM Baru</h1>
      <p class="text-secondary mb-0" style="font-size: 14px;">Mendaftarkan warung UMKM dan akun admin pengelolanya</p>
    </div>
    <div>
      <a href="/admin-system/umkm" class="btn btn-outline-secondary d-flex align-items-center gap-2" style="border-radius: 8px; padding: 10px 20px;">
        <i class="bi bi-arrow-left"></i>
        <span>Kembali</span>
      </a>
    </div>
  </div>

  <div class="row fade-in">
    <div class="col-12 col-lg-10 col-xl-8">
      <div class="glass-card p-4">
        <%- include('../../partials/alerts') %>

        <form action="/admin-system/umkm" method="POST">
          <input type="hidden" name="_csrf" value="<%= csrfToken %>">
          <!-- UMKM Profile Section -->
          <h3 class="h6 fw-bold text-primary mb-3 border-bottom pb-2">1. Data Profil UMKM</h3>
          
          <div class="mb-3">
            <label class="form-label fw-semibold text-secondary">Nama UMKM / Warung</label>
            <input type="text" name="nama_umkm" class="form-control form-control-custom <%= errors.nama_umkm ? 'is-invalid' : '' %>" value="<%= old.nama_umkm || '' %>" required placeholder="Contoh: Toko Berkah Jaya">
            <% if (errors.nama_umkm) { %>
              <div class="invalid-feedback"><%= errors.nama_umkm %></div>
            <% } %>
          </div>

          <div class="row mb-3">
            <div class="col-md-6">
              <label class="form-label fw-semibold text-secondary">Nomor Telepon</label>
              <input type="text" name="telepon" class="form-control form-control-custom <%= errors.telepon ? 'is-invalid' : '' %>" value="<%= old.telepon || '' %>" placeholder="Contoh: 08123456789">
              <% if (errors.telepon) { %>
                <div class="invalid-feedback"><%= errors.telepon %></div>
              <% } %>
            </div>
            <div class="col-md-6">
              <label class="form-label fw-semibold text-secondary">Alamat Lengkap</label>
              <textarea name="alamat" class="form-control form-control-custom <%= errors.alamat ? 'is-invalid' : '' %>" rows="1" placeholder="Contoh: Jl. Diponegoro No. 12"><%= old.alamat || '' %></textarea>
              <% if (errors.alamat) { %>
                <div class="invalid-feedback"><%= errors.alamat %></div>
              <% } %>
            </div>
          </div>

          <!-- Admin Credentials Section -->
          <h3 class="h6 fw-bold text-primary mt-4 mb-3 border-bottom pb-2">2. Akun Admin Pengelola</h3>

          <div class="row mb-3">
            <div class="col-md-6">
              <label class="form-label fw-semibold text-secondary">Username (ID Login)</label>
              <input type="text" name="username" class="form-control form-control-custom <%= errors.username ? 'is-invalid' : '' %>" value="<%= old.username || '' %>" required placeholder="Contoh: owner_berkah">
              <% if (errors.username) { %>
                <div class="invalid-feedback"><%= errors.username %></div>
              <% } %>
            </div>
            <div class="col-md-6">
              <label class="form-label fw-semibold text-secondary">Nama Lengkap Admin</label>
              <input type="text" name="name" class="form-control form-control-custom <%= errors.name ? 'is-invalid' : '' %>" value="<%= old.name || '' %>" required placeholder="Contoh: H. Ahmad Sulaiman">
              <% if (errors.name) { %>
                <div class="invalid-feedback"><%= errors.name %></div>
              <% } %>
            </div>
          </div>

          <div class="row mb-4">
            <div class="col-md-6">
              <label class="form-label fw-semibold text-secondary">Alamat Email</label>
              <input type="email" name="email" class="form-control form-control-custom <%= errors.email ? 'is-invalid' : '' %>" value="<%= old.email || '' %>" placeholder="Contoh: owner@berkah.com">
              <% if (errors.email) { %>
                <div class="invalid-feedback"><%= errors.email %></div>
              <% } %>
            </div>
            <div class="col-md-6">
              <label class="form-label fw-semibold text-secondary">Password Akses</label>
              <input type="password" name="password" class="form-control form-control-custom <%= errors.password ? 'is-invalid' : '' %>" required placeholder="Minimal 6 karakter">
              <% if (errors.password) { %>
                <div class="invalid-feedback"><%= errors.password %></div>
              <% } %>
            </div>
          </div>

          <button type="submit" class="btn btn-primary-custom w-100 py-3 fw-semibold">
            <i class="bi bi-save me-1"></i> Simpan UMKM & Admin
          </button>
        </form>
      </div>
    </div>
  </div>
</div>

<%- include('../../partials/footer') %>
